Crear caperta del sistema
    mkdir inv_qr 
    cd inv_qr

2. Crear entorno virtual
    python3 -m venv venv
source venv/bin/actívate

export DB_SERVER="invqr.chii2mgga6uz.us-east-2.rds.amazonaws.com"
export DB_NAME="invqr"
export DB_USER="admin"
export DB_PASSWORD="3nviaBuca"

3. Instalar las librerías
     pip install fastapi uvicorn sqlalchemy pyodbc openpyxl jinja2 python-multipart passlib bcrypt python-jose

4. Crear carpetas internas
     mkdir app
     mkdir templates
     mkdir static

5. Creación de archivos
nano templates/crear.html -> Pega el código -> Guarda/Sal.
nano templates/index.html -> Pega el código -> Guarda/Sal.
nano templates/ficha.html -> Pega el código -> Guarda/Sal.
nano templates/login.html -> Pega el código -> Guarda/Sal.
nano templates/editar.html -> Pega el código -> Guarda/Sal.
nano templates/parametros.html -> Pega el código -> Guarda/Sal.
nano templates/editar_parametro.html -> Pega el código -> Guarda/Sal.
nano app/main.py
nano app/database.py
nano app/models.py